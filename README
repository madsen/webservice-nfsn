WebService-NFSN version {{$version}}, released {{$date}}


WebService::NFSN is a client library for NearlyFreeSpeech.NET's member
API (https://members.nearlyfreespeech.net/wiki/API).  It is only
useful to people who have websites hosted at NearlyFreeSpeech.NET.



INSTALLATION

To install this module, run the following commands:

    perl Build.PL
    ./Build
    ./Build test
    ./Build install



DEPENDENCIES

{{ my $deps = $t->dependency_list;
   $deps =~ s/^(\s*JSON::XS\s.+)/$1  (JSON 2.0 is also acceptable)/m;
   $deps;
}}

Also, some of the example scripts use Data::Dumper to display the results.



CHANGES
    Here's what's new in version {{$version}} of WebService-NFSN:
    (See the file "Changes" for the full revision history.)

{{$changes}}



COPYRIGHT AND LICENCE

{{ local $_ = $zilla->license->notice; chomp; $_ }}
